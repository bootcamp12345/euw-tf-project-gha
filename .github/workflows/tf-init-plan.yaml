on:
    workflow_dispatch:
    # push:
    #   branches:
    #     - main

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: test
              run: |
                  echo "test"